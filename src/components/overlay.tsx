export function Overlay() {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "100vh",
        color: "white",
        fontFamily: "sans-serif",
        background: "rgba(30, 30, 30, 0.55)",
        borderRadius: 8,
      }}
    >
      Overlay spike: position / transparency / focus check
    </div>
  );
}
