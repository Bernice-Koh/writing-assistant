{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions granted to the main and overlay windows.",
  "windows": ["main", "overlay"],
  "permissions": ["core:default"]
}
